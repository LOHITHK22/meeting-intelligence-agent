# ── Core ──────────────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9        # required for FastAPI file uploads
pydantic==2.7.1

# ── Transcription ─────────────────────────────────────────────────────────────
openai-whisper==20231117       # local Whisper model
# Alternative: use the OpenAI API instead (faster, no GPU needed locally)
# openai==1.30.1

# ── Speaker diarization ───────────────────────────────────────────────────────
pyannote.audio==3.3.2
torch==2.3.1                   # pyannote.audio dependency

# ── Storage (Phase 2 will add SQLAlchemy + PostgreSQL) ───────────────────────
# sqlalchemy==2.0.30
# psycopg2-binary==2.9.9

# ── Dev / testing ─────────────────────────────────────────────────────────────
httpx==0.27.0                  # for async test client
pytest==8.2.0
pytest-asyncio==0.23.7
